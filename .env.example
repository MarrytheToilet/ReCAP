# Copy to .env and fill in local credentials.
# Do not commit real API keys.

OPENAI_API_KEY=

# Optional. Use this for OpenAI-compatible providers.
# OPENAI_BASE_URL=https://api.openai.com/v1

# Required for `--agent openai` unless you pass `--model`.
RECAP_LLM_MODEL=
